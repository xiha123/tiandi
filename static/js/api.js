require=(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({"api":[function(require,module,exports){
var apiConfig = {"jquery":"jQuery","isSuccess":{"status":true},"ignoreResponse":false,"context":"window","timeout":5000,"success":["data"],"fail":["error"],"outputFile":""},
    $ = jQuery,
    ajaxStatus = {};

/**
 * @param {String} config.need
 * @param {Object} config.target
 * @param {String} [config.value]
 * @param {Promise} config.promise
 */
function checkParam(config) {
    var value = $.trim(config.target[config.need]);

    if (config.value && value !== config.value) {
        return false;
    }
    if (!config.value && value === '') {
        config.promise.reject('请求失败: 缺少参数' + config.need);
        return false;
    }
    config.target[config.need] = value;

    return true;
}

/**
 * @param {Object} config.need
 * @param {Object} config.target
 * @param {Promise} config.promise
 */
function checkParams(config) {
    for (var key in config.need) {
        if (!config.need.hasOwnProperty(key)) continue;
        if (!checkParam({
            need: key,
            target: config.target,
            promise: config.promise,
            value: config.need[key]
        })) {
            return false;
        }
    }

    return true;
}

function getParams(target, params) {
    var result = {},
        len = params.length;

    while (len--) {
        if (target[params[len]] === undefined && !apiConfig.ignoreResponse) {
            return false;
        }
        result[params[len]] = target[params[len]];
    }

    return result;
}

/**
 * @param {String} config.name
 * @param {Object} config.url
 * @param {Object} config.data
 * @param {String} config.type
 * @param {Object} config.context
 * @param {Number} config.timeout
 * @param {Promise} config.promise
 * @param {Array} config.successParam
 * @param {Array} config.failParam
 */
function doAjax(config) {
    ajaxStatus[config.name] = true;
    return $.ajax({
        cache: false,
        data: config.data,
        dataType: 'json',
        type: config.type,
        context: config.context,
        url: config.url,
        timeout: config.timeout,
        success: function (res, status, xhr) {
            var result, params, cb;

            if (checkParams({
                need: apiConfig.isSuccess,
                target: res,
                promise: config.promise
            })) {
                params = config.successParam;
                cb = config.promise.resolve;
            } else {
                params = config.failParam;
                cb = config.promise.reject;
            }

            result = getParams(params, res);
            if (result === false) {
                throw new Error('请求失败: 返回结果中无预期参数');
            }

            cb(result);
        },
        error: function () {
            config.promise.reject('请求失败: 网络不稳定或服务器繁忙');
        },
        complete: function () {
            delete ajaxStatus[config.name];
        }
    });
}

            exports.loginAdmin = function (data) {                if (ajaxStatus["loginAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "loginAdmin",                    url: "api/admin_api/login",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.logoutAdmin = function (data) {                if (ajaxStatus["logoutAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                                if (!isPass) return promise;                return doAjax({                    name: "logoutAdmin",                    url: "api/admin_api/logout",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createAdmin = function (data) {                if (ajaxStatus["createAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createAdmin",                    url: "api/admin_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.editAdmin = function (data) {                if (ajaxStatus["editAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "editAdmin",                    url: "api/admin_api/edit",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.removeAdmin = function (data) {                if (ajaxStatus["removeAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "removeAdmin",                    url: "api/admin_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.loginUser = function (data) {                if (ajaxStatus["loginUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "loginUser",                    url: "api/user_api/login",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.logoutUser = function (data) {                if (ajaxStatus["logoutUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                                if (!isPass) return promise;                return doAjax({                    name: "logoutUser",                    url: "api/user_api/logout",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createUser = function (data) {                if (ajaxStatus["createUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "email",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createUser",                    url: "api/user_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.editUser = function (data) {                if (ajaxStatus["editUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "editUser",                    url: "api/user_api/edits",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.removeUser = function (data) {                if (ajaxStatus["removeUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "removeUser",                    url: "api/user_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.eye = function (data) {                if (ajaxStatus["eye"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "user_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "eye",                    url: "api/user_api/eye",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.onlineSave = function (data) {                if (ajaxStatus["onlineSave"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "tags",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "code",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "language",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "onlineSave",                    url: "api/problem_api/online_save",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createProblem = function (data) {                if (ajaxStatus["createProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "detail",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "tags",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "code",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "coinType",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "language",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createProblem",                    url: "api/problem_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createDetail = function (data) {                if (ajaxStatus["createDetail"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "code",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "language",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createDetail",                    url: "api/problem_api/create_detail",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createComment = function (data) {                if (ajaxStatus["createComment"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createComment",                    url: "api/problem_api/create_comment",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.requestProblem = function (data) {                if (ajaxStatus["requestProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "requestProblem",                    url: "api/problem_api/request_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.closeProblem = function (data) {                if (ajaxStatus["closeProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "closeProblem",                    url: "api/problem_api/close_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.followProblem = function (data) {                if (ajaxStatus["followProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "followProblem",                    url: "api/problem_api/follow_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.unfollowProblem = function (data) {                if (ajaxStatus["unfollowProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "unfollowProblem",                    url: "api/problem_api/unfollow_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.collectProblem = function (data) {                if (ajaxStatus["collectProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "collectProblem",                    url: "api/problem_api/collect_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.uncollectProblem = function (data) {                if (ajaxStatus["uncollectProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "uncollectProblem",                    url: "api/problem_api/uncollect_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.upProblem = function (data) {                if (ajaxStatus["upProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "upProblem",                    url: "api/problem_api/up_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.downProblem = function (data) {                if (ajaxStatus["downProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "downProblem",                    url: "api/problem_api/down_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createCourse = function (data) {                if (ajaxStatus["createCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "video",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createCourse",                    url: "api/course_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.removeCourse = function (data) {                if (ajaxStatus["removeCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "removeCourse",                    url: "api/course_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.editCourse = function (data) {                if (ajaxStatus["editCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "video",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "editCourse",                    url: "api/course_api/edit",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createChapter = function (data) {                if (ajaxStatus["createChapter"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createChapter",                    url: "api/course_api/create_chapter",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.createStep = function (data) {                if (ajaxStatus["createStep"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "img",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "level",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "createStep",                    url: "api/course_api/create_step",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.editChapter = function (data) {                if (ajaxStatus["editChapter"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "chapter_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "editChapter",                    url: "api/course_api/edit_chapter",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };                    exports.editStep = function (data) {                if (ajaxStatus["editStep"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "step_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "img",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "level",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                return doAjax({                    name: "editStep",                    url: "api/course_api/edit_step",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });            };        

},{}]},{},[]);
