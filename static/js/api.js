require=(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({"api":[function(require,module,exports){
var apiConfig = {"jquery":"jQuery","isSuccess":{"status":true},"ignoreResponse":false,"context":"window","timeout":5000,"success":["data"],"fail":["error"],"outputFile":""},
    ajaxStatus = {};

/**
 * @param {String} config.need
 * @param {Object} config.target
 * @param {String} [config.value]
 * @param {Promise} config.promise
 */
function checkParam(config) {
    var value = config.target[config.need];
    value = typeof value === 'string' ? jQuery.trim(value) : value;

    if ((config.value !== undefined && value !== config.value) || (config.value === undefined && value === undefined)) {
        config.promise.reject('请求失败: 缺少参数' + config.need);
        return false;
    }
    config.target[config.need] = value;

    return true;
}

/**
 * @param {Object} config.need
 * @param {Object} config.target
 */
function isSuccess(config) {
    for (var key in config.need) {
        if (!config.need.hasOwnProperty(key)) continue;
        if (config.target[key] !== config.need[key]) {
            return false;
        }
    }

    return true;
}

function getParams(target, params) {
    var result = {},
        len = params.length;

    while (len--) {
        if (target[params[len]] === undefined && !apiConfig.ignoreResponse) {
            return false;
        }
        result[params[len]] = target[params[len]];
    }

    return result;
}

/**
 * @param {String} config.name
 * @param {Object} config.url
 * @param {Object} config.data
 * @param {String} config.type
 * @param {Object} config.context
 * @param {Number} config.timeout
 * @param {Promise} config.promise
 * @param {Array} config.successParam
 * @param {Array} config.failParam
 */
function doAjax(config) {
    ajaxStatus[config.name] = true;
    jQuery.ajax({
        cache: false,
        data: config.data,
        dataType: 'json',
        type: config.type,
        context: config.context,
        url: config.url,
        timeout: config.timeout,
        success: function (res, status, xhr) {
            var result, params, cb;

            if (isSuccess({
                need: apiConfig.isSuccess,
                target: res
            })) {
                params = config.successParam;
                cb = config.promise.resolve;
            } else {
                params = config.failParam;
                cb = config.promise.reject;
            }

            result = getParams(res, params);
            if (result === false) {
                throw new Error('请求失败: 返回结果中无预期参数');
            }

            cb(result);
        },
        error: function () {
            config.promise.reject('请求失败: 网络不稳定或服务器繁忙');
        },
        complete: function () {
            delete ajaxStatus[config.name];
        }
    });
}
            exports.forget = function (data) {                if (ajaxStatus["forget"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "email",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "verification",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "forget",                    url: "api/user_api/forget",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.chou = function (data) {                if (ajaxStatus["chou"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "chou",                    url: "api/problem_api/chou",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.removeTag = function (data) {                if (ajaxStatus["removeTag"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "removeTag",                    url: "api/tag_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editTag = function (data) {                if (ajaxStatus["editTag"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editTag",                    url: "api/tag_api/edit",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.loginAdmin = function (data) {                if (ajaxStatus["loginAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "loginAdmin",                    url: "api/admin_api/login",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.logoutAdmin = function (data) {                if (ajaxStatus["logoutAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                                if (!isPass) return promise;                doAjax({                    name: "logoutAdmin",                    url: "api/admin_api/logout",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createAdmin = function (data) {                if (ajaxStatus["createAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createAdmin",                    url: "api/admin_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editAdmin = function (data) {                if (ajaxStatus["editAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editAdmin",                    url: "api/admin_api/edit",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.removeAdmin = function (data) {                if (ajaxStatus["removeAdmin"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "removeAdmin",                    url: "api/admin_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.checkOauth = function (data) {                if (ajaxStatus["checkOauth"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "key",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "checkOauth",                    url: "api/user_api/check_oauth",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.loginUser = function (data) {                if (ajaxStatus["loginUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "loginUser",                    url: "api/user_api/login",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.logoutUser = function (data) {                if (ajaxStatus["logoutUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                                if (!isPass) return promise;                doAjax({                    name: "logoutUser",                    url: "api/user_api/logout",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createUser = function (data) {                if (ajaxStatus["createUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "email",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "pwd",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "avatar",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createUser",                    url: "api/user_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editUser = function (data) {                if (ajaxStatus["editUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "nickname",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editUser",                    url: "api/user_api/edits",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.removeUser = function (data) {                if (ajaxStatus["removeUser"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "removeUser",                    url: "api/user_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.eye = function (data) {                if (ajaxStatus["eye"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "user_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "eye",                    url: "api/user_api/eye",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.onlineSave = function (data) {                if (ajaxStatus["onlineSave"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "tags",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "code",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "language",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "onlineSave",                    url: "api/problem_api/online_save",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createProblem = function (data) {                if (ajaxStatus["createProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "coinType",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "language",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createProblem",                    url: "api/problem_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.removeProblem = function (data) {                if (ajaxStatus["removeProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "removeProblem",                    url: "api/problem_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createDetail = function (data) {                if (ajaxStatus["createDetail"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "language",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createDetail",                    url: "api/problem_api/create_detail",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createComment = function (data) {                if (ajaxStatus["createComment"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createComment",                    url: "api/problem_api/create_comment",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.requestProblem = function (data) {                if (ajaxStatus["requestProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "requestProblem",                    url: "api/problem_api/request_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.closeProblem = function (data) {                if (ajaxStatus["closeProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "closeProblem",                    url: "api/problem_api/close_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.collectProblem = function (data) {                if (ajaxStatus["collectProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "collectProblem",                    url: "api/problem_api/collect_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.uncollectProblem = function (data) {                if (ajaxStatus["uncollectProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "uncollectProblem",                    url: "api/problem_api/uncollect_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.upProblem = function (data) {                if (ajaxStatus["upProblem"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "upProblem",                    url: "api/problem_api/up_problem",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.getProblemInfo = function (data) {                if (ajaxStatus["getProblemInfo"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "getProblemInfo",                    url: "api/problem_api/get_info",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createCourse = function (data) {                if (ajaxStatus["createCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "video",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createCourse",                    url: "api/course_api/create",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.removeCourse = function (data) {                if (ajaxStatus["removeCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "removeCourse",                    url: "api/course_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editCourse = function (data) {                if (ajaxStatus["editCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "video",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editCourse",                    url: "api/course_api/edit",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createChapter = function (data) {                if (ajaxStatus["createChapter"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createChapter",                    url: "api/course_api/create_chapter",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.createStep = function (data) {                if (ajaxStatus["createStep"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "course_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "img",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "level",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "createStep",                    url: "api/course_api/create_step",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editChapter = function (data) {                if (ajaxStatus["editChapter"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "chapter_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editChapter",                    url: "api/course_api/edit_chapter",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editStep = function (data) {                if (ajaxStatus["editStep"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "step_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "description",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "img",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "level",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editStep",                    url: "api/course_api/edit_step",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.editSite = function (data) {                if (ajaxStatus["editSite"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "type",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "editSite",                    url: "api/site_api/update",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.getNewProblems = function (data) {                if (ajaxStatus["getNewProblems"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "page",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "getNewProblems",                    url: "api/problem_api/get_new_problems",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.getHotProblems = function (data) {                if (ajaxStatus["getHotProblems"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "page",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "getHotProblems",                    url: "api/problem_api/get_hot_problems",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.getFundProblems = function (data) {                if (ajaxStatus["getFundProblems"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "page",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "getFundProblems",                    url: "api/problem_api/get_fund_problems",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.syncGodAnswer = function (data) {                if (ajaxStatus["syncGodAnswer"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "problem_id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "syncGodAnswer",                    url: "api/problem_api/get_god_temp_answer",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.addTag = function (data) {                if (ajaxStatus["addTag"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "name",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "content",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "addTag",                    url: "api/tag_api/add",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.getNews = function (data) {                if (ajaxStatus["getNews"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                                if (!isPass) return promise;                doAjax({                    name: "getNews",                    url: "api/news_api/get_news",                    data: data || {},                    type: "get",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.addGodCourse = function (data) {                if (ajaxStatus["addGodCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "title",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "img",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "link",                target: data,                promise: promise            }) === false) {                isPass = false;            }                    if (checkParam({                need: "god",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "addGodCourse",                    url: "api/god_course_api/add",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };                    exports.removeGodCourse = function (data) {                if (ajaxStatus["removeGodCourse"]) return false;                var promise = jQuery.Deferred(),                    isPass = true;                            if (checkParam({                need: "id",                target: data,                promise: promise            }) === false) {                isPass = false;            }                        if (!isPass) return promise;                doAjax({                    name: "removeGodCourse",                    url: "api/god_course_api/remove",                    data: data || {},                    type: "post",                    context: window,                    timeout: 5000,                    promise: promise,                    successParam: ["data"],                    failParam: ["error"]                });                return promise;            };        

},{}]},{},[]);
